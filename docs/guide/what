
@project-name assumes that signatures are derived from the @italic{raw bytes}
of the digest. This means that if you create a signature from an ANS.1-encoded
digest using a command like @litchar{openssl dsgt -sign ...}, then
@project-name will see a signature mismatch.
