#lang info

(define provider-name "you")
(define package-name "no-deps")
(define edition-name "draft")
(define revision-number 0)
(define revision-names '("initial"))
(define installer "installer.rkt")
(define dependencies '())
